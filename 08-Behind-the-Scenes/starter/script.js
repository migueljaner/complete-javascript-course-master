'use strict';

/* function calcAge(birthYear) {
  const age = 2037 - birthYear;

  function printAge() {
    const output = `${firstName}, You are ${age}, born in ${birthYear}`;
    console.log(output);

    if (birthYear >= 1981 && birthYear <= 1996) {
      var millenial = ture;
      const firstName = 'Steven';
      const str = `Oh, and you're a millenial, ${firstName}`;
      console.log(str);

      function add(a, b) {
        //Funciones solo son block-Scoped en 'strict mode'
        return a + b;
      }

      output = 'NEW OUTPUT';
    }
    console.log(millenial);
    // console.log(add(2, 3)); //Solo funciona si 'stricmode' no esta activado
    console.log(output);
  }
  printAge();
  return age;
}

const firstName = 'Jhonas';
calcAge(1991); */
